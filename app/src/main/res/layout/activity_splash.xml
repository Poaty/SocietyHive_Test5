<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="?attr/colorPrimary">

    <!-- colorPrimary background means the splash screen colour matches whatever theme the user has selected -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SocietyHive"
        android:textSize="36sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnPrimary" />

    <!-- alpha 0.7 dims the tagline so it reads as a subtitle without needing a separate colour token -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="Your societies, all in one place."
        android:textSize="14sp"
        android:textColor="?attr/colorOnPrimary"
        android:alpha="0.7" />

</LinearLayout>
